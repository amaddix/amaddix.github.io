{
  "name": "codeshell-server",
  "version": "1.0.0",
  "description": "CodeShell C++ compiler proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}